# ProcessGuard Pro — Python Dependencies
# Install via: pip install -r requirements.txt
# Or use: bash scripts/install.sh  (handles apt packages + venv too)

# ── Core GUI ──────────────────────────────────────────────────────────────────
dearpygui>=1.11.1          # GPU-accelerated Dear ImGui Python bindings (main UI)

# ── Process Monitoring ────────────────────────────────────────────────────────
psutil>=5.9.8              # Cross-platform process & system stats (CPU/RAM/Disk)

# ── Notifications (Linux desktop alerts) ─────────────────────────────────────
# Requires system package: sudo apt install libnotify-bin
notify2>=0.3.1             # Desktop notification support (optional but recommended)

# ── System Tray Icon (optional) ───────────────────────────────────────────────
pystray>=0.19.5            # System tray icon support
Pillow>=10.3.0             # Image handling required by pystray

# ── Standard Library (no install needed) ─────────────────────────────────────
# sqlite3   — process history & stats DB  (built-in)
# json      — config.json read/write      (built-in)
# threading — background scanner thread   (built-in)
# queue     — thread-safe GUI updates     (built-in)
# re        — regex whitelist & rules     (built-in)
# logging   — alert/kill/autokill logs    (built-in)
# datetime  — timestamps                  (built-in)
# pathlib   — file paths                  (built-in)
